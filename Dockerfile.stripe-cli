# Use the official Stripe CLI image
FROM stripe/stripe-cli:latest

# Set environment variables
ENV STRIPE_API_KEY=""

# Expose the port that Stripe CLI uses for webhook forwarding
EXPOSE 13111

# Use the default entrypoint but override the command
CMD ["listen", "--forward-to", "http://backend:3000/webhook", "--print-secret"] 